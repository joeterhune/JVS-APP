systemctl restart httpd  "Restart apache"
killall httpd  "Kill apache process"

ps -eaf | grep httpd  "force restart"
find / -type d -name "dir-name-here"

Changing to apache user and shell

1)	Change the shell for the apache user to /bin/bash (it defaults to /sbin/nologin, which will prevent you from getting a shell) 
a.	% chsh apache
2)	Become the apache user 
a.	% su -l apache
